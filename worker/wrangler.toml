name = "realdebrid-magnet-worker"
main = "src/index.js"
compatibility_date = "2025-12-04"
nodejs_compat = true

[vars]
# Non-sensitive variables can go here

# Secret variables should be set via: wrangler secret put REAL_DEBRID_API_KEY
# Environment variables are accessed via env.VARIABLE_NAME in the Worker
